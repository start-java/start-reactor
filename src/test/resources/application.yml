spring:
  main:
    banner-mode: 'off'
logging:
  file: target/test.log
  level: # TRACE < DEBUG < INFO < WARN < ERROR < FATAL < OFF
    root: WARN
    tech.simter.start.reactor: WARN
    io.reactor: WARN
